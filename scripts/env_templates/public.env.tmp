#=====================================Project meta===========================================================
PROJECT_NAME=AufruttenK8sCICDTemplate

# Email address for notifications and register the certificate for domain
EMAIL_ADDRESS=''

#============================================HOSTS===========================================================
# The debug hosts change it in your wish. But should to finish in .local (it's better to leave it as is)
# And the change the production domain in your.
# Debug always must end with .local  example: realYourDomain.local api.realYourDomain.local
# example.local api.example.local
# First: API Second: UI

BACKEND_HOST=api.example.com
DEBUG_BACKEND_HOST=api.server.local

FRONTEND_HOST=example.com
DEBUG_FRONTEND_HOST=server.local


#===============================================VITE=========================================================
# Do not touch here nothing! Because that is pointers for externals apps like vue.js
VITE_PROJECT_NAME=${PROJECT_NAME}
VITE_BACKEND_HOST=${BACKEND_HOST}
VITE_DEBUG_BACKEND_HOST=${DEBUG_BACKEND_HOST}
