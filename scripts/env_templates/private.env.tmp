#===================================================Docker=============================================================

#---------------------------|<<===---------the--key--should--be--here----===>|'
SECRET_KEY='django-insecure-|===================change=======================|'

# Postgres Database
# Do not change if you doesn't have external database!!!
POSTGRES_HOST=postgres-svc
POSTGRES_PORT=5432
POSTGRES_NAME=AirERPSystemDatabase
POSTGRES_USER=django
POSTGRES_DB=${POSTGRES_NAME}
POSTGRES_PASSWORD=${SECRET_KEY}

# Email
EMAIL_PASSWORD=''

# Google OAuth2 (https://console.cloud.google.com/apis)
# Remember what you should to enable the API People on Google OAuth2, else it doesn't work
GOOGLE_CLIENT_ID=''
GOOGLE_CLIENT_SECRET=''

# Cloudinary (https://cloudinary.com)
CLOUD_NAME=''
CLOUD_API_KEY=''
CLOUD_API_SECRET=''

#===============================================Docker==(CI/CD)========================================================
# It use only to setup the project once. In K8s install (setup)
# Username - where will store your image on docker hub and configured your secrets GitHub
# This username must match the one stored in GitHub secrets.
# If you are not the maintainer or owner of that project you isn't require to insert the username
# IT MUST BE IN LOWERCASE!!!
DOCKER_USERNAME=''

# Docker token must be installed with only read access (If you have closed images on Dokcer hub (Close access))
# That field could be empty if your images is in open access
# TODO: Put into use (Is not yet use)
DOCKER_TOKEN=''
